# API base URL (empty = same-origin, e.g. proxy on Cloudflare Pages)
VITE_API_BASE=

# File upload server (empty = same origin)
VITE_UPLOAD_BASE=

# WebSocket URL (empty = auto-detected from hostname)
VITE_WS_URL=
