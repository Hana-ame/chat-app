# API base URL (empty = same origin, proxied via reverse proxy)
VITE_API_BASE=
# Upload service base URL
VITE_UPLOAD_BASE=https://upload.moonchan.xyz
# WebSocket URL (empty = auto-detect from location.protocol + location.host)
VITE_WS_URL=
