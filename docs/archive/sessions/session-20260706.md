# 会话摘要 — 2026-07-06

## 1. 分析 Git 历史
- 检查是否有 FastAPI 相关代码 → 无, 后端为 Go (chi), 仅 README 提了一次 `chat-app-fastapi.pages.dev`

## 2. 分析当前上传设计
- 后端: `POST /api/uploads` (Go handler, MIME 白名单, 随机 key 存储, 大小限制, JWT 鉴权)
- 前端: `api.upload` 走外部 `upload.moonchan.xyz` (附件), `api.uploadAvatar` 走后端 (头像)
- 外部服务 `upload.moonchan.xyz` 不可用导致上传失败

## 3. 头像上传改为直接走外部服务
- 修改 `client/src/api/client.js` → `uploadAvatar` 不再走后端, 改 `PUT upload.moonchan.xyz/api/upload`
- 测试脚本 `client/tests/upload_test.sh` (curl)
  - 上传文本/Png 均返回 HTTP 200 + id
  - 删除不符预期的测试用例
  - 最终 3/3 通过

## 4. 维护 README
- `client/tests/README.md`: 测试目录说明
- `client/src/README.md`: 源码结构、架构、状态管理、上传、路由、滚动行为、dev 工具
- `client/src/COMPOSITION_REVIEW.md`: 组件组合分析 (ChatList 过重, 重复代码, 缺失组件)
- `client/src/api/README.md`: API 方法表、错误处理
- 根 `README.md` 添加注意事项: 每次修改后必须 `git push`

## 5. 组件组合分析
- ChatList (313 行) 职责过重 → 建议拆分 SettingsModal, UserAvatar, useUserSearch hook
- ChatView.loadMore 调用 `api.listMessages` 但未合并到 store → load older 功能不可用
- `getDMName` 在 ChatList 和 ChatView 中重复定义
- `hooks/` 目录为空 → 建议提取 useRealtime 和 useMobile hook

## 6. 添加 Dummy Data 调试工具
- `client/src/dev/dummy.js`: 生成 5 chat × 40 msg 模拟数据 (Markdown, 附件, reaction, 删除)
- ChatList 底部 `🧪 Generate test data` 按钮
- 构建通过

## 7. API 模块整理
- 按 Auth / Chats / Members / Messages / Reactions / Uploads / Misc 分组
- 提取 `buildUploadUrl()` 辅助函数
- `uploadAvatar` 委托 `api.upload`
- 创建 `client/src/api/README.md`

## 8. GitHub Actions 编译 Go 二进制
- 增加 `go-build` job: matrix 编译 linux/amd64, linux/arm64, windows/amd64
- `release` job 收集三个二进制 + client/dist → `chatd-release` 构件

## 9. 注意事项
- 每次修改代码或文档后必须 `git push`, Cloudflare Pages 自动部署前端
- 根 README 已添加 `⚠️ 重要：每次修改后必须 Push` 章节
